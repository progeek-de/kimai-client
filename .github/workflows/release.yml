name: release-tag

on:
  push:
    tags:
      - '*'
permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: "Setup Java"
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: "Setup Gradle"
        uses: gradle/gradle-build-action@v2

      - name: "Setup GO"
        uses: actions/setup-go@v4
        with:
          go-version: '>=1.20.1'

      - name: "Checkout project sources"
        uses: actions/checkout@v3

      - name: "Get Metadata - Version"
        id: meta
        run: |
          echo REPO_VERSION=$(git describe --tags --always | sed 's/^v//') >> $GITHUB_OUTPUT
          echo ZIP_FILE=$(kimai-linux-x64-${REPO_VERSION}.zip) >> $GITHUB_OUTPUT

      - name: "Create Release"
        uses: actions/create-release@v1
        id: create_release
        with:
          draft: false
          prerelease: false
          release_name: ${{ steps.meta.outputs.REPO_VERSION }}
          tag_name: ${{ github.ref }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: "Build"
        run: ./gradlew --no-daemon -PprojServer="https://kimai.cloud" -PprojVersion="${{ steps.meta.outputs.REPO_VERSION }}" -Pbuildkonfig.flavor=release :kimai-desktop:packageReleaseUberJarForCurrentOS

      - name: "Zip"
        uses: montudor/action-zip@v1.0.0
        with:
          args: zip -qq -r -j kimai-linux-x64-${{ steps.meta.outputs.REPO_VERSION }}.zip ./kimai-desktop/build/compose/jars

      - name: "Upload artifact"
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./kimai-linux-x64-${{ steps.meta.outputs.REPO_VERSION }}.zip
          asset_name: kimai-linux-x64-${{ steps.meta.outputs.REPO_VERSION }}.zip
          asset_content_type: application/zip